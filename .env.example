# ============================================================
# AI Ops NeoBankingOperation Platform — CENTRAL RUNTIME CONFIG
# ============================================================
# Copy this file to .env for local/dev/test runtime configuration.
# .env is intentionally gitignored. Never commit real credentials.
# Kubernetes/OpenShift should populate the same keys through Secret/ConfigMap.

# -------------------- Application -----------------------------
APP_NAME=AI Ops NeoBankingOperation Platform
APP_VERSION=0.1.0
APP_ENV=development
DEBUG=False
HOST=0.0.0.0
PORT=8000

# -------------------- PostgreSQL / Alembic -------------------
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/aiops
ALEMBIC_DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/aiops
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# -------------------- LLM -------------------------------------
# mock is development/test only. Production validation rejects it.
LLM_PROVIDER=mock
LLM_API_KEY=
LLM_BASE_URL=
LLM_MODEL=gpt-4
LLM_TIMEOUT_SECONDS=60

# -------------------- Embeddings / pgvector ------------------
# deterministic is development/test only. Production must use an internal/offline gateway.
EMBEDDING_PROVIDER=deterministic
EMBEDDING_BASE_URL=
EMBEDDING_API_KEY=
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSION=1536
EMBEDDING_TIMEOUT_SECONDS=30
PGVECTOR_EXPECTED_DIMENSION=1536
PGVECTOR_VALIDATE_ON_STARTUP=True

# -------------------- Knowledge governance -------------------
KNOWLEDGE_ALLOWED_SOURCE_TYPES=["runbook","sop","architecture","service_catalog","operations_standard","troubleshooting"]
KNOWLEDGE_REQUIRE_GOVERNANCE_PRODUCTION=True

# -------------------- Agent operational policy ---------------
# Agents only analyze/recommend. They never execute write actions directly.
AGENT_LLM_TEMPERATURE=0.2
AGENT_MAX_EVIDENCE_ITEMS=40
AGENT_MIN_EVIDENCE_ITEMS=1
AGENT_LOW_CONFIDENCE_THRESHOLD=0.55
AGENT_MAX_RECOMMENDATIONS=5
AGENT_MAX_HYPOTHESES=4

# -------------------- Logging / API / Security ---------------
LOG_LEVEL=INFO
LOG_JSON=False
INTERNAL_API_KEY=
INTERNAL_API_ROLE=operator
API_RATE_LIMIT_PER_MINUTE=100
RATE_LIMIT_STRICT_REQUESTS=20
RATE_LIMIT_LOOSE_REQUESTS=500
RATE_LIMIT_WINDOW_SECONDS=60
RETRY_MAX_ATTEMPTS=3
RETRY_DELAY_SECONDS=1.0
RETRY_BACKOFF_FACTOR=2.0
CORS_ORIGINS=["*"]
APPROVAL_TTL_SECONDS=3600

# -------------------- Zabbix ----------------------------------
ZABBIX_URL=http://localhost:8080
ZABBIX_USERNAME=
ZABBIX_PASSWORD=
ZABBIX_TIMEOUT_SECONDS=10

# -------------------- Elasticsearch ---------------------------
ELASTICSEARCH_HOSTS=["http://localhost:9200"]
ELASTICSEARCH_USERNAME=
ELASTICSEARCH_PASSWORD=
ELASTICSEARCH_TIMEOUT_SECONDS=10

# -------------------- Prometheus ------------------------------
PROMETHEUS_URL=http://localhost:9090
PROMETHEUS_TIMEOUT_SECONDS=10

# -------------------- VM / SSH --------------------------------
SSH_ENABLED=False
SSH_USERNAME=
SSH_PRIVATE_KEY_PATH=
SSH_KNOWN_HOSTS=
SSH_STRICT_HOST_KEY_CHECKING=True
SSH_PORT=22
SSH_CONNECT_TIMEOUT=10
VM_CPU_RECOVERY_THRESHOLD=70

# -------------------- OIDC / SSO ------------------------------
OIDC_ISSUER_URL=
OIDC_AUDIENCE=
OIDC_JWKS_URL=

# -------------------- Offline deployment ---------------------
OFFLINE_IMAGE_REGISTRY=
IMAGE_PULL_POLICY=IfNotPresent
